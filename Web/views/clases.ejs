<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("header", {titulo : "Clases"}) %>
</head>
<body>
    <div class="container">
        <div class="row">
            <div class="col text-center mt-5">
                <h2>Resultados generales</h2>
            </div>
        </div>
        <div class="row justify-content-center mt-4">
            <% institutos.forEach(i => { %>
                <div class="col-sm-6 mb-3 mb-sm-0">
                    <div class="card">
                        <div class="card-body">
                        <h5 class="card-title"><%= i.nombre %></h5>
                        <a href="/profesor/generarDatos/<%= i.id %>" class="btn btn-primary">Ver resultados sesión</a>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
</body>
</html>