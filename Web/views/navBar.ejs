<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">Articoding</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/profesor/resumen">Resultados</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profesor/alumnos">Alumnos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profesor/categorias">Categorías</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profesor/comparativa">Comparativa</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/profesor/errores">Errores</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/logout"><i class="fa-solid fa-gear fa-xl"></i></a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<nav aria-label="breadcrumb" class="mt-3 ms-5 fw-bold fs-6">
    <ol class="breadcrumb" id="migaDePan"> 
    </ol>
</nav>

<script>
    $(document).ready(function(){
        var url = window.location.href;
        var matches = url.match(/(?<!\/\/)\/\w+/g);
        var words = matches.map(function(match) {
            return match.substring(1);
        });

        var acum = "";
        for(var i = 1; i < words.length; i++){
            if(i == (words.length - 1)){
                $("#migaDePan").append('<li class="breadcrumb-item active" aria-current="page">' + words[i].charAt(0).toUpperCase() + words[i].substring(1) + '</li>');
            }
            else{
                acum += "/" + words[i];
                $("#migaDePan").append('<li class="breadcrumb-item"><a href="'+ acum +'">' + words[i].charAt(0).toUpperCase() + words[i].substring(1) + '</a></li>');
            }
        }
    });
</script>